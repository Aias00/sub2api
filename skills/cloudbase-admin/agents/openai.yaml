interface:
  display_name: "Cloudbase Admin"
  short_description: "Manage Cloudbase admin accounts and settings"
  default_prompt: "Use $cloudbase-admin to inspect and manage Cloudbase backend accounts safely."

policy:
  allow_implicit_invocation: true
